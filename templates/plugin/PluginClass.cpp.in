#include "{{CLASS_NAME}}.h"
#include <QVBoxLayout>
#include <QLabel>

{{CLASS_NAME}}::{{CLASS_NAME}}(QWidget *parent)
    : QWidget(parent)
{
    setup_ui();
}

QString {{CLASS_NAME}}::name() const
{
    return "{{PLUGIN_NAME}}";
}

QString {{CLASS_NAME}}::version() const
{
    return "{{VERSION}}";
}


QWidget* {{CLASS_NAME}}::create(QWidget *parent)
{
    return new {{CLASS_NAME}}(parent);
}
