#include <QObject>
#include <QtPlugin>

// 模拟写死的基类接口 (实际项目中建议 #include "IPlugin.h")
class IPlugin {
public:
    virtual ~IPlugin() = default;
    virtual void initialize() = 0;
};
Q_DECLARE_INTERFACE(IPlugin, "com.vibe.IPlugin/1.0")

#include "{{class_name}}.h"

class {{class_name}}Wrapper : public QObject, public IPlugin
{
    Q_OBJECT
    Q_INTERFACES(IPlugin)
    Q_PLUGIN_METADATA(IID "com.vibe.IPlugin")

public:
    void initialize() override {
        // 实例化用户在 metadata.json 中指定的类
        auto* instance = new {{class_name}}();
        // 此处可添加更多通用初始化逻辑
    }
};
